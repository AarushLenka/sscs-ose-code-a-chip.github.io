# TRIAL RUN ONLY -- do not commit, will be deleted after clock period is derived
# Same as ol_conservative_uniform.yaml but with CLOCK_PERIOD: 20.0 ns so the
# critical path delay can be read from the slack report (PD_GUIDE.md section 9).

PDK: sky130A
DESIGN_NAME: fir_conservative_uniform
VERILOG_FILES:
  - dir::src/verilog/rtl/fir_conservative_uniform.v

CLOCK_PORT: clk
CLOCK_PERIOD: 20.0          # deliberately loose -- read wns to find critical path

DIE_AREA: [0, 0, 500, 500]
CORE_AREA: [10, 10, 490, 490]

PL_TARGET_DENSITY_PCT: 50
SYNTH_STRATEGY: AREA 0
